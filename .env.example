# Copy to .env locally. On Vercel, set these in Project → Settings → Environment Variables.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional AI / voice (app still loads without them; some features degrade)
VITE_GEMINI_API_KEY=
VITE_GROQ_API_KEY=
SARVAM_API_KEY=
